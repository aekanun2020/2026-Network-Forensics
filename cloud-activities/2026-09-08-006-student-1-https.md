# Prepare public HTTPS MCP on student-1

- Started UTC: 2026-09-08T15:07:58.295354+00:00
- Actor: Codex / thaimcpagent@gmail.com using existing OS Login sudo access.
- Project / VM / zone: bigdatainpractice1 / student-1 / asia-southeast1-a.
- Authorization: user selected HTTPS for Claude Desktop, then explicitly said “ไม่ต้องกังวลเรื่อง login แยกสิทธิ์ครับ ส่งมาเลย”. No login or per-person isolation is required for this deployment at the user's direction.
- Verified before change: VM RUNNING, external IP 34.142.187.162, no network tags, all 8 existing lab containers healthy; only port 22 has a wildcard host listener. MCP is on loopback 8001. DNS 34-142-187-162.sslip.io resolves to the VM IP.
- Firewall read: default-allow-http allows TCP 80 from 0.0.0.0/0 for tag http-server; default-allow-https allows TCP 443 from 0.0.0.0/0 for tag https-server. Neither tag currently applies to this VM. The user retains control over firewall and tag changes.
- Planned mutation: stage Caddyfile and Compose HTTPS service in /opt/nf01, pull and pin the real Caddy image, validate configuration, create one TLS ingress container with persistent certificate storage. Forward the original MCP Streamable HTTP transport to the existing real server; no protocol translator or replacement MCP implementation. Preserve all existing loopback bindings and evidence files. No GCP VPC firewall, VM tag, IAM, VM/disk, or static IP changes.
- External dependency: sslip.io DNS and Let's Encrypt ACME; the hostname embeds the current IP. Domain is not owned by the user and the VM IP has not been established as reserved. A changed VM external IP requires reconfiguration and a new endpoint. A public certificate will disclose the hostname in certificate transparency logs.
- Exposure: user requested unauthenticated access; callers reaching /mcp can invoke the same shared tools, including Spark job submission. No per-person boundary is claimed.
- Cost: existing VM operation plus one bounded 0.5 CPU / 256 MiB container, image/certificate storage and network traffic. No additional paid GCP resource provisioned.
- Cleanup: remove only the https service and its two named certificate/config volumes if requested; do not prune other services or volumes.
- Status: configuration preparation; certificate issuance/public verification pending user firewall action.

## Image pull

`docker pull caddy:2` succeeded; resolved digest `sha256:df7f1c2fb114453b951de51a98efc010db1655a92c2e86be6706714e2417a78d`. Compose pinned to this digest before service creation.

## Automatic approval review blocked service start

At 2026-09-08T15:09:36.677903+00:00, the command combining installation of the two staged config files, validation and HTTPS service startup was rejected before execution. The visible review reason identifies unauthenticated public access to evidence and Spark submission as unacceptable risk without sufficiently specific confirmation. No HTTPS service or public port binding was created by that rejected command. User was asked explicitly to confirm public access to raw evidence and shared Spark execution. Existing MCP remains loopback-only. Both configuration files were successfully copied to /tmp on the VM before rejection; Caddy image pull had succeeded. Do not infer that /opt/nf01 configuration installation or certificate issuance has completed.

## Independent validation

`docker run --rm --network none --read-only -v /tmp/Caddyfile:/etc/caddy/Caddyfile:ro <pinned-image> caddy validate --config /etc/caddy/Caddyfile --adapter caddyfile` returned `Valid configuration`. This transient validation container had no network or published ports and was removed automatically. Subsequent container inventory showed exactly the existing 8 healthy lab containers with loopback-only port bindings. This is configuration validation, not a TLS handshake, certificate issuance, public MCP check or Claude Desktop test.
